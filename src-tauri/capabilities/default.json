{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main Sift window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
